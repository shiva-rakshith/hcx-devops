apiVersion: v1
kind: Service
metadata:
  name: {{ include "hcx_mock_service.fullname" . }}
  labels:
    {{- include "hcx_mock_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hcx_mock_service.selectorLabels" . | nindent 4 }}
