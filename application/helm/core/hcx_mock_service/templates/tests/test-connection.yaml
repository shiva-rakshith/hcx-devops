apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hcx_mock_service.fullname" . }}-test-connection"
  labels:
    {{- include "hcx_mock_service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
