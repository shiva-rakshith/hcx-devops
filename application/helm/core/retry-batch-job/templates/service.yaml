apiVersion: v1
kind: Service
metadata:
  name: {{ include "retry-batch-job.fullname" . }}
  labels:
    {{- include "retry-batch-job.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retry-batch-job.selectorLabels" . | nindent 4 }}
