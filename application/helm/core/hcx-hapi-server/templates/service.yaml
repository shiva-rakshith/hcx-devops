apiVersion: v1
kind: Service
metadata:
  name: {{ include "hcx-hapi-server.fullname" . }}
  labels:
    {{- include "hcx-hapi-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "hcx-hapi-server.selectorLabels" . | nindent 4 }}
